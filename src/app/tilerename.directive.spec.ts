import { TilerenameDirective } from './tilerename.directive';

describe('TilerenameDirective', () => {
  it('should create an instance', () => {
    const directive = new TilerenameDirective();
    expect(directive).toBeTruthy();
  });
});
